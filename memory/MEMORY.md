# Memory Index

- [Per-component variables](per-component-variables.md) — how variablesSchema → config.json `variables` works; chained-fallback CSS convention for new components.
- [From FastStore](from-faststore.md) — the /from-faststore command that ingests a .starter component into the catalog preview (registry + layoutData + derived variablesSchema).
- [Catálogo × faststore.starter](../docs/CATALOGO-E-FASTSTORE.md) — por que este catálogo existe e como achar, para cada preview, o componente real no repo irmão (o campo `path`).
